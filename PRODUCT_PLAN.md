# ContextoTO · 安卓篇章英语学习软件策划案

版本：v0.1 · 2026-09-23 · 状态：开发中的原始策划记录

实现调整（2026-09-24）：应用户补充要求，长按分析单位由自然段改为句子；浮层展示整句释义、句内从句范围与词语简义，目录统计“已查询句”。句子浮层的英文原句比正文略大，并随正文比例设置变化。下文保留初版策划思路，若与此调整冲突，以本段和实际实现为准。

## 1. 产品定位

面向中文母语、以考研英语阅读为主要场景的学习者。应用把一篇英文文章当作完整的学习单位：先连续阅读，遇到障碍再查词或拆段；查询结果贴着原文出现，关闭后立即回到原阅读位置。第一版的核心体验是“阅读不中断”。

首次进入打开第 1 篇；以后每次启动直接回到上次文章和阅读位置。48 篇全文离线可读，已获得的释义也离线可看。首版不加入打卡、背词卡、排行榜、账户系统或音频，避免分散阅读界面的视觉重心。

## 2. 已核实的预置资产

| 资产 | 实际内容 | 首版用途 |
|---|---:|---|
| `kaoyan-corpus-48.tar.gz` | 48 篇、58,815 词；30 篇改写、18 篇定向；正文共 323 个自然段 | 只抽取标题、正文和篇号，预置进 APK |
| `kaoyan-wordlists.tar.gz/final-3000.csv` | 3,000 个唯一词条 | 主背词表、排序、静态释义与真题统计 |
| `kaoyan-wordlists.tar.gz/kaoyan-exam-freq.csv` | 4,801 个唯一词条 | 考纲词库与 S/A/B/C/D/X 频级 |
| `kaoyan-wordlists.tar.gz/gap-319.csv` | 319 个唯一词条，均在上述两表中 | 零语料词标识 |

文章 Markdown 包含审阅指标、正文、篇末目标词表；App 只展示正文和适合读者看的标题/篇号。`sources/` 是对照源文，不进入 APK。词表中的 `translation` 是 ECDICT 原始多义释义，可立即离线显示；它不能自动指出某一句实际取的义项。词形归并和词义选择必须分开处理。预置包保留在仓库，构建时生成一份紧凑的文章/词库数据，避免把压缩包、解压件和数据库三份同时打入 APK。

## 3. 首版阅读路径

1. 启动即恢复 `articleId + paragraphId + 段内滚动偏移`，正文先出现；后台立即对当前可见、尚无分析缓存的段落发起一次 LLM 预热请求。若已缓存，选择下一未分析段落；网络、密钥不可用时仍能阅读并显示静态词表释义。
2. 短按一个英文词，原词轻微抬升，词旁出现无边框的磨砂释义层。已缓存时立即显示；未缓存时立即发起高优先级查询，浮层切换为进度态，结果到达后在同一位置替换。文章滚动时浮层跟随锚点；锚点离屏后收起。
3. 长按一个词，以其所在**自然段**为单位弹出段落层。原段从正文位置抬升并移动到页面上部，层内可滚动；展示段落中文意思、从句起点和覆盖范围、该段中命中词库或已查询词的简义。长按不触发短按查询。
4. 从左侧边缘向右拖出文章目录；目录宽度为屏宽的 80%，正文整体向右平移约 60% 屏宽，保留原排版宽度，形成 20% 的前后层叠。目录可垂直滚动并切换篇章。此处按“目录从左出现、正文向右让位”确定滑动方向；轻点正文外露区域、向左回拖或系统返回键关闭。
5. 任一弹层出现时，轻点其外的磨砂区域即弹回原位。每次只允许一个主弹层；系统返回键先关闭弹层，再退出阅读。

目录每行包含篇号、短标题、上次阅读标记，以及两项分色数字：`已查询单词`按本篇去重词元（lemma）计数，`已查询段落`按自然段 ID 计数。静态词库命中或后台预热不算“已查询”；只有用户主动短按/长按且请求成功或命中有效缓存才记入，反复点按不重复累计。目录顶部把最近阅读篇章置为明显视觉焦点，其他篇章保持原顺序。

## 4. 三种浮层的信息设计

### 4.1 单词释义层

视觉层级从上到下为：大号粗体原词和词形 → **本句取义**（最大中文字号、强调色）→ 词库归属与频级 → 其他义项 → 派生词。词库标签可同时出现“主背 3000”“考纲 4801”“零语料 319”；`rank`、`band`、`exam_freq` 仅在有值时作为次级信息显示。静态 CSV 释义保留原文并按词性/斜杠拆为可读义项；本句取义由模型结合原句给出，并明确标在被选中的义项旁。若模型给出的上下文义与静态词表无法安全对齐，单列为“本句用法”，不伪装成词典原义。

派生词按“派生 / 词形变化”区分，最多显示 4 个与本词及当前语境相关的项；无可信结果时整个区块隐藏。专有名词、复合词、撇号所有格按原文词形保留，查询时再归并词元。

### 4.2 段落解析层

顶部显示段号和该段全文，下面是整段中文释义。英文段落中的从句用 3–4 种低饱和色循环标出范围，在各从句起始处放细小色标和序号；下方按同一序号列出从句类型、英文片段和简短中文关系说明。嵌套从句允许范围重叠，用左侧窄色带和起止标记保留层级，不让多重底色盖住文字。命中预置词库与曾查词只做轻量字色强调，段底按出现顺序列“原词 · 本句简义 · 词库标识”；其余词不强行列出。模型对从句边界没有足够把握时显示句子拆分和翻译，隐藏错误的彩色范围。

### 4.3 查询进度层

沿用单词或段落浮层的原位置、磨砂材质和内容宽度，显示三个阶段：`拼接上下文`、`等待首字返回`、`接收完整结果`。阶段切换和进度填充使用缓动。拼接与本地校验可显示真实进度；服务端等待使用流动的不确定进度轨道，不捏造百分比。流式 `reasoning_content` 到来时显示“推理中 ≈ N tokens”；这里的 N 是本地实时估算，结束后用响应 `usage.completion_tokens_details.reasoning_tokens` 的准确值替换。若兼容服务不提供思考流或 usage，显示“正在生成”及已接收字节数。超时、断网、JSON 校验失败提供简洁错误与重试，保留用户原点击位置。

## 5. 视觉与动效规范

英文正文指定 Times New Roman，建议 21sp、1.48 行距、左右 24dp 留白；中文回退到能覆盖汉字的衬线字体。标题 26–30sp，释义重点 23sp 加粗，次级说明 12–14sp。阅读页只保留篇号/标题、正文和极轻的目录入口；词库标记默认不铺满全文，用户操作后才显现。

明亮主题：纸色 `#F2F0EA`、主字 `#262B29`、次字 `#66716E`；暗色主题：底色 `#171C1B`、主字 `#E8E7E1`、次字 `#A7B0AB`。单词使用低饱和松绿 `#5D7B72`，段落统计使用陶土色 `#A87562`，从句辅色选灰紫和赭黄；实际落地以对比度和模拟器截图微调。所有形状直角、纯色、无描边；重点通过字号、粗细、空间与少量色彩建立，不使用渐变玻璃卡片、圆角按钮或厚重阴影。

弹层本身只有强磨砂背景与轻微半透明主题色调制，不叠加第二层不透明卡片。背景正文需真实模糊，而不是仅把前景文字模糊；实现时把正文绘制到独立图层，模糊其在浮层下方的区域，再绘制浮层内容。磨砂建议从 0 过渡到约 20–28dp 半径，文字对比不够时仅调整同一磨砂层的色调与透明度。Android 12 起支持相应图形效果，首版最低系统拟定 API 31；在模拟器和低性能设备上以帧率实测调节半径。

单词层进入/退出约 200–260ms，原词与浮层共享位置过渡；段落层约 300–380ms，保留原段的空间来源感；目录使用阻尼弹簧，位移和磨砂同步。进度填充使用 ease-out，阶段跳变不倒退。系统开启“减少动画”时压缩位移与模糊过渡，保留状态清晰度。

**字体交付条件**：Times New Roman 在 Android 上不是可默认依赖的系统字体；微软说明 Windows 随附字体不能直接随 App 再分发。开发预览可先用尺寸接近的合法衬线字体调布局，正式 APK 要使用具有 App 再分发许可的 Times New Roman 字体文件。字体授权是精确满足品牌字形要求的交付项，不从 Windows 系统目录直接复制进公开仓库。

## 6. 数据模型与处理规则

文章在构建时生成稳定的 `articleId`、`paragraphId`、原文字符偏移和内容 SHA-256；句子/英文 token 按字符区间索引。点击命中依据 Compose 文本布局返回的字符偏移，再映射到 token；撇号、连字符、跨行换行、斜体 Markdown、标点邻接都要单独验证。自然段以正文空行分割，不能把 Markdown 元数据或末尾词表混入阅读内容。

本地存储分为：只读预置库（48 篇及三份词表）、用户库（阅读位置、主动查询记录、模型结果缓存、主题及字号）和密钥设置。缓存键包含 `文章/段落/词元/原句哈希/提示词版本/模型名`；段落解析含从句范围和译文。源文变化或提示词更新时只让相关缓存失效。词形识别优先通过预置词表与常见屈折规则建立候选，最终义项始终由原句决定。

表结构建议：`Article`、`Paragraph`、`Lexeme`、`WordOccurrence`、`WordAnalysis`、`ParagraphAnalysis`、`LookupEvent`、`ReadingPosition`。`LookupEvent` 只记录主动查询成功事件；两项目录数字由去重查询得到，不存易漂移的手写累计值。

## 7. LLM 调用设计

首版只实现 OpenAI Chat Completions 兼容的 `POST /chat/completions`、SSE 流、`response_format: {"type":"json_object"}`。默认 DeepSeek 官方地址使用 `model: "deepseek-flash"`、`thinking: {"type":"enabled"}`、`reasoning_effort: "max"`；用户所说的“deepseek-v4.1flash-max”在产品配置界面显示为 **DeepSeek V4.1 Flash · Max**。官方当前文档没有列出同名字串作为模型 ID，所以底层字段按官方 ID 与推理强度发送。兼容端点可改 base URL、模型 ID 和密钥，但第一版不实现 Anthropic/Responses 协议。

开发测试优先使用已提供的 Command Code GOAT 测试凭据：兼容 base URL 为 `https://api.commandcode.ai/provider/v1`，该服务公开列出的 V4.1 Flash 模型 ID 为 `deepseek/deepseek-v4.1-flash`。先用其 `/models` 与最小 Chat 请求核实这把凭据的实际权限、`reasoning_effort: max`、JSON 与 SSE 支持，再跑大量内容和界面联调。生产默认配置与最终少量端到端验收改用 DeepSeek 官方 `https://api.deepseek.com` / `deepseek-flash`。测试配置与生产配置只在运行时切换，接口仍是同一套 Chat Completions 解析代码。

查询分两类：启动预热/段落长按调用 `paragraph_analysis`，返回段落中文译文、逐句/从句范围和词库命中词的简义；短按调用 `word_analysis`，输入目标词、所在句、前后句与文章标题，返回本句取义、其他义项及派生关系。若已有段落分析，短按请求引用其精简上下文；用户主动查询优先于预热，允许两者并行但每类同一缓存键只发一次请求。只预热当前可见段落，不在启动时扫描 48 篇。

格式化返回值采用版本化 JSON。示意：

```json
{
  "schema_version": 1,
  "type": "word_analysis",
  "target": "state",
  "context_sense": {"zh": "陈述", "part_of_speech": "verb", "evidence": "states that"},
  "other_senses": [{"zh": "状态", "part_of_speech": "noun"}],
  "derivatives": [{"word": "statement", "relation": "派生名词", "zh": "声明"}]
}
```

段落响应含 `paragraph_id`、`translation_zh`、`clauses[]`（`sentence_id`、`start`、`end`、`kind`、`brief_zh`）与 `glosses[]`（token 起止、`brief_zh`）。模型返回的范围只作为候选，客户端必须核对偏移是否在原文内、引用文本是否吻合、是否对应目标 token；不合格时尝试一次局部修复或降级为无彩色范围的译文。内容解析、词库归属和计数均在本地完成，不能由模型自由改写。提示词要求严格 JSON、中文释义、不要额外文章内容；在 `finish_reason=length` 或 JSON 不完整时不落库。

首次使用需配置 API 密钥；不在 APK、Git、日志中写入密钥。密钥使用 Android Keystore 保护的本地加密存储，网络仅走 HTTPS。若未来需要给普通用户免配置提供服务，应增加受控代理，不能把统一服务密钥打入安装包。发送请求前清楚说明文章片段会发送到所配置的模型服务。

## 8. 技术路线与空间安排

Android 原生 Kotlin + Jetpack Compose，阅读层使用可获取字形位置的文本布局和自定义命中测试；Room/SQLite 保存查询与进度，DataStore 保存轻量设置，OkHttp 处理 SSE，Kotlin 序列化与显式校验构造数据。Compose 动效管理浮层状态，但背景磨砂先做一个 API 35 模拟器原型验证“正文模糊、弹层清晰”的绘制次序，再定最终实现。

本机已检测到 Android SDK（`D:\Android\Sdk`）、两个 API 35 AVD，其中一个模拟器在线；E 盘约有 172GB 可用，而 C/D 盘各只剩约 2GB。正式开发时把 `GRADLE_USER_HOME`、Android 构建输出、AVD/快照及大体积临时解包目录迁到 E 盘；已有 SDK 先复用，不重复下载系统镜像。仓库只放源代码、原始压缩包、轻量生成脚本和文档，构建产物由 `.gitignore` 排除。当前工作目录尚不是 Git checkout；接入已存在的同名 GitHub 仓库前先核对远端内容与本地文件，避免覆盖。

## 9. 开发顺序与验收

| 阶段 | 可验收结果 |
|---|---|
| A. 数据底座 | 48 篇正文、323 段、三份词表导入；标题/段落/词元定位与词库归属自动检查通过 |
| B. 阅读与视觉原型 | 浅色/深色正文、文章目录 80%/60% 错层、短按词与长按段、真实背景磨砂及回弹动画在 API 35 模拟器可操作 |
| C. LLM 与缓存 | 启动预热、即时查询、三阶段进度、格式校验、缓存、离线回看、失败重试；官方 DeepSeek 配置可用 |
| D. 收尾验证 | 冷启动恢复原位；48 篇可读；单词/段落计数不重复；断网、慢流、空结果、旋转/后台恢复、深浅主题、长段落和系统返回键验证 |

首版完成标准：读者能在 48 篇中连续阅读；短按得到本句义项与词库标签，长按得到段意与可靠的从句范围；所有查询层可点磨砂区收起；目录状态与阅读位置重启后仍正确；模型不可用时全文和预置词典仍可使用。对照模拟器录屏检查浮层进入/退出的空间连续性，并对长段落磨砂滚动做性能抽查。

## 10. 待定的产品输入

1. **Times New Roman 字体授权**：正式发布前需取得可在 Android APK 中分发的字体文件及许可；没有时开发样机使用合法衬线替代字体验证排版。
2. **模型连接**：用户已提供测试与官方生产凭据。两者只在本地运行时注入，不写进文档、源码、Git 配置、日志、截图或 APK；开发阶段主要消耗测试额度，官方额度只用于最终验收。若任一凭据失效，应用仍可用预置词表离线阅读。

## 参考

- [DeepSeek V4.1 Flash 发布与官方模型名](https://api-docs.deepseek.com/updates/)
- [DeepSeek Chat Completions：JSON、SSE、usage](https://api-docs.deepseek.com/api/create-chat-completion/)
- [DeepSeek 思考模式与 reasoning_effort](https://api-docs.deepseek.com/guides/thinking_mode/)
- [Command Code Provider API 地址与 OpenAI Chat 兼容格式](https://commandcode.ai/docs/provider)
- [Command Code 可用模型 ID](https://commandcode.ai/docs/reference/cli/models)
- [Android Compose 图形层与模糊效果](https://developer.android.com/reference/kotlin/androidx/compose/ui/graphics/layer/GraphicsLayer)
- [Android API 密钥保护建议](https://developer.android.com/privacy-and-security/security-tips)
- [Microsoft 字体再分发常见问题](https://learn.microsoft.com/en-us/typography/fonts/font-faq)
